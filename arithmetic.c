#include <stdio.h>
int main (){
    // Arithmetic operators
    // + (addition)
    // - (subtruction)
    // * (multiplication)
    // / (division)
    // % (modulus)
    // ++ (increment)
    // -- (decrement)

    //EXAMPLE

    int x = 5;
    int y = 2;

    //int z = x+y;
    //int z = x-y;
    //int z = x*y;
    //float z = x/(float)y;
    //int z = x%y;
    //int z = y --;
    int z = x--;

    printf("%d\n", y);

    return 0;

}